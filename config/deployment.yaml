apiVersion: apps/v1
kind: Deployment
metadata:
    name: homepage
    namespace: default
    labels:
        app: homepage
spec:
    replicas: 2
    selector:
        matchLabels:
            app: homepage
    template:
        metadata:
            labels:
                app: homepage
        spec:
            containers:
            - name: server
              image: kirandasika30/au-hacks-landing
              imagePullPolicy: Always
              ports:
              - containerPort: 8321
              args:
              - --listen_addr=:8321
              resources:
                requests:
                  cpu: "20m"
                  memory: "50Mi"
                limits:
                  cpu: "50m"
                  memory: "100Mi"

