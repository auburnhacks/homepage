apiVersion: v1
kind: Service
metadata:  
  name: homepage-svc
  annotations:
    traefik.backend.circuitbreaker: "NetworkErrorRatio() > 0.5"
spec:
  selector:    
    app: homepage
  ports:  
  - name: http
    port: 80
    targetPort: 8321 
    protocol: TCP
